# 产品需求文档（PRD）

🛑 本文档由大脑 PM 在 final_output 阶段生成。严格遵守此模板。

## 一、基本信息

| 属性 | 内容 |
|------|------|
| 项目名称 | ... |
| 品类 | ... |
| 商业模式 | ... |
| 目标用户 | ... |
| 品牌定位 | ... |
| 一期结论 | 可交付/需补充 |

## 二、模块清单与分期

🛑 先列主干，再列枝干。每个模块一行横向表格。分期列统一填"一期"。

### 主干模块（N 个）

| 编号 | 模块 | 定义(s) | 分期(p) | 前台能力(f) | 后台能力(b) | 边界契约 | 数据实体与约束(d) | 验收标准(t) | 创新与成本(i/c) |
|------|------|---------|---------|-------------|-------------|---------|-------------------|-------------|-----------------|
| M1 | **模块名** | ... | 一期 | ... | ... | 输入:xxx 输出:xxx 触发:同步API | ... | ... | i: 无 / c: ¥0 |

🛑 **边界契约**格式：`输入:{依赖模块的数据/事件} 输出:{提供给谁的数据/事件} 触发:{同步API/异步消息/定时任务}`。不涉及跨模块交互的模块写 `无跨模块依赖`。
🛑 **创新与成本(i/c)**：涉及第三方服务（支付/短信/物流/OAuth/OSS/邮件/搜索）时，必须标注 `双模式: mock/real` + 预估月费。格式：`c: ¥200/月 (第三方物流API, 双模式: mock/real)`。不涉及第三方服务的写 `i: 无 / c: ¥0/月`。

### 枝干模块（N 个）

| 编号 | 模块 | 定义(s) | 分期(p) | 前台能力(f) | 后台能力(b) | 边界契约 | 数据实体与约束(d) | 验收标准(t) | 创新与成本(i/c) |
|------|------|---------|---------|-------------|-------------|---------|-------------------|-------------|-----------------|
| M5 | **Search** `←Discovery` | ... | 一期 | ... | ... | 输入:xxx 输出:xxx 触发:xxx | ... | ... | i: 无 / c: ¥0 |

🛑 表头与主干表完全一致。枝干的模块列用 `**名称** \`←父模块\`` 格式标注依附关系。
🛑 禁止竖表（字段\|内容两列）。禁止在二章内列二期模块。
🛑 **前台能力(f)列必须列出具体页面路径**（如 `购物车页(src/pages/cart.vue)`），不能只写"购物车管理"。这是后续 UI 设计和架构任务生成的唯一页面来源。

## 三、核心业务流程

### 核心用户主链路（Happy Path）
*(根据项目命名角色，如：观众/创作者/买家/商家)*

步骤1 → 步骤2 → ...

🛑 每个等待/异步节点标注超时时间。例：`→ 支付(30min超时自动取消) →`

### 关键异常分支
🛑 至少列出 3 条异常分支，覆盖最常见的失败场景：

| 异常场景 | 触发条件 | 处理流程 | 涉及模块 |
|---------|---------|---------|---------|
| 支付失败 | 网关超时/余额不足 | 跳转重试页 → 30min后自动取消 → 释放库存 | M3/M4 |
| 库存不足 | 下单时库存被抢完 | 订单创建失败 → 提示"已售罄" → 推荐相似商品 | M2/M3 |
| ... | ... | ... | ... |

### 平台运营主链路

步骤1 → 步骤2 → ...

## 四、关键决策与业务规则

*(根据项目实际情况选择子标题。不适用则删除。不要照搬电商模板到非电商项目)*

参考子标题：审核体系 / 逆向流程与SLA / 权限矩阵 / 权限校验点清单 / 数据所有权与合规 / 资金与结算（仅交易类项目）/ 全局配置常量表

### 权限校验点清单
🛑 列出核心接口与所需角色的对应关系：

| 接口/页面 | 所需角色 | 校验方式 |
|----------|---------|---------|
| POST /api/orders | 用户 | JWT + owner check |
| GET /api/admin/users | 管理员 | JWT + role guard |
| ... | ... | ... |

### 全局配置常量表
🛑 统一收录散落在各模块的配置项：

| 配置项 | 默认值 | 作用模块 | 可热更新 |
|--------|-------|---------|---------|
| 订单支付超时 | 30 分钟 | M3/M4 | 是 |
| 购物车过期时间 | 30 天 | M5 | 否 |
| ... | ... | ... | ... |

## 五、数据模型概要

### 实体关系总览
🛑 核心实体清单 (架构师解析器依赖此行, 格式: `表名 (中文名)`):
users (用户), videos (视频), comments (评论), ...
*(ASCII 关系图: users --1:N--> videos ... )*

### 核心查询场景与索引建议
🛑 列出高频查询及对应索引：

| 查询场景 | SQL 模式 | 建议索引 | 涉及模块 |
|---------|---------|---------|---------|
| 用户订单列表 | WHERE user_id=? ORDER BY created_at DESC | (user_id, created_at) | M3 |
| 分类商品筛选 | WHERE category_id=? AND status=? | (category_id, status) | M2 |
| ... | ... | ... | ... |

### 关键数据约束
- UNIQUE / 外键级联 / 状态机流转 / 加密与脱敏

### 核心状态机流转
*(用文本箭头描述关键实体的状态流转。🛑 覆盖逆向分支: 退款/退货/取消的状态回退路径)*

## 六、非功能性需求

| 维度 | 要求 |
|------|------|
| 性能 | ... |
| 并发与容量 | ... |
| 安全 | ... |
| 可用性 | ... |
| 兼容性 | ... |
| 扩展性 | ... |
| 降级策略 | 关键依赖不可用时的兜底方案（如支付网关超时→手动补单；物流API失败→商家手动录入） |

## 七、待决策问题

### 已决议

| 问题 | 决议 | 理由 |
|------|------|------|

### 已延期（二期触发条件）

🛑 只列有明确量化触发条件的项。最多列 3 项（早期项目 0 项）。模糊条件（"等用户多了"）的项直接删除，不列在此。

| 问题 | 默认建议 | 触发条件 |
|------|----------|----------|

### 风险登记册
🛑 至少列出 3 个已知风险：

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 乐观锁并发冲突导致超卖 | 中 | 高 | 数据库唯一约束兜底 + 定期对账 |
| ... | ... | ... | ... |

### 测试策略摘要
🛑 简述验收方式：

| 维度 | 要求 |
|------|------|
| 端到端验收 | 沙箱环境跑通 N 笔完整订单 |
| 核心接口覆盖率 | > 80% |
| 集成测试范围 | 支付回调 / 库存扣减 / 超时取消 |
