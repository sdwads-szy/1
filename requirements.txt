# =============================================================================
# Agent 多智能体代码生成系统 — Python 依赖
# =============================================================================
# 安装: pip install -r requirements.txt
# =============================================================================

# ── Agent 框架（含 LLM 客户端）──
picoagents>=0.1.0

# ── 环境变量 ──
python-dotenv>=1.0.0

# ── 向量数据库 (RAG 知识库) ──
lancedb>=0.6.0
pyarrow>=14.0.0

# ── Embedding 模型 ──
sentence-transformers>=3.0.0
numpy>=1.24.0

# ── 缓存（可选，不可用时自动降级为文件存储）──
redis>=5.0.0

# ── 重排序（可选，不可用时跳过 rerank 阶段）──
# FlagEmbedding>=1.2.0
